overwrite: true
schema: "http://adam:test@localhost:8080/graphql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      # - "typescript-resolvers"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
